// [SEMANTICS] assigning to loop counter is forbidden — ERROR
// Expect: error "loop variable is read-only"

routine Main() is
  for i in 1..3 loop
    i := 10     // forbidden
    print i
  end
end