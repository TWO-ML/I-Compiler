# Семантический Анализатор (Semantic Analyzer)

Реализация семантического анализатора для языка Project I с проверками и оптимизациями AST.

## Реализованные возможности

### Проверки (не модифицируют AST) ✓

#### 1. Объявление переменных и функций до использования
- Проверяет, что все переменные и функции объявлены перед использованием
- Поддерживает вложенные области видимости
- Обнаруживает использование необъявленных идентификаторов

**Пример ошибки:**
```pascal
routine Main() is
  x := 5          // ОШИБКА: x используется до объявления
  var x : integer
end
```

**Вывод:**
```
Error at 2:3: variable 'x' used before declaration
```

#### 2. Проверка корректного использования return
- Проверяет, что `return` используется только внутри функций
- Обнаруживает `return` в глобальной области видимости

**Пример ошибки:**
```pascal
routine Main() is
  print 5
end

return 10  // ОШИБКА: return вне функции
```

**Вывод:**
```
Error at 5:1: return statement can only be used inside a routine
```

#### 3. Базовая проверка типов аргументов функций
- Проверяет количество аргументов при вызове функции
- Сравнивает с ожидаемым числом параметров

### Оптимизации (модифицируют AST) ✓

#### 1. Свёртка констант (Constant Folding)
Упрощает константные выражения во время компиляции.

**Поддерживаемые операции:**
- Арифметические: `+`, `-`, `*`, `/`, `%`
- Сравнения: `<`, `<=`, `>`, `>=`, `=`, `/=`
- Логические: `and`, `or`, `xor`, `not`

**Примеры:**
```pascal
var a : integer is 5 + 3        // -> 8
var b : integer is 10 * 2       // -> 20
var c : boolean is 3 < 5        // -> true
var d : boolean is true and false // -> false
var e : boolean is not false    // -> true
```

**Статистика:**
```
Свёртка констант: 5 операций
```

#### 2. Удаление недостижимого кода (Unreachable Code Removal)
Удаляет код, который никогда не будет выполнен (например, после `return`).

**Пример:**
```pascal
routine test() : integer is
  var x : integer is 5
  return x
  var y : integer is 10    // УДАЛЯЕТСЯ
  print y                  // УДАЛЯЕТСЯ
end
```

**Статистика:**
```
Удалено недостижимого кода: 2 блоков
```

#### 3. Упрощение условных конструкций (If Simplification)
Упрощает `if` с константными условиями.

**Примеры:**

До оптимизации:
```pascal
if true then
  print 1
  print 2
else
  print 3
end
```

После оптимизации:
```pascal
print 1
print 2
```

До оптимизации:
```pascal
if false then
  print 4
else
  print 5
end
```

После оптимизации:
```pascal
print 5
```

**Статистика:**
```
Упрощение if: 2 конструкций
```

#### 4. Удаление неиспользуемых переменных (Dead Code Elimination)
Удаляет переменные, которые объявлены, но нигде не используются.

**Пример:**
```pascal
routine Main() is
  var a : integer is 5
  var b : integer is 10   // УДАЛЯЕТСЯ: не используется
  var c : integer is 15   // УДАЛЯЕТСЯ: не используется
  
  print a
end
```

**Статистика:**
```
Удалено неиспользуемых переменных: 2
```

## Сборка и запуск

### Сборка
```bash
make build/semantic_analyzer
```

### Запуск на одном файле
```bash
./build/semantic_analyzer tests/001_expressions.pi
```

### Демонстрация всех возможностей
```bash
chmod +x run_semantic_tests.sh
./run_semantic_tests.sh
```

## Структура проекта

```
I-Compiler/
├── src/
│   ├── lexer.cpp                  # Лексический анализатор
│   ├── parser.cpp                 # Синтаксический анализатор
│   └── semantic_analyzer.cpp      # Семантический анализатор
├── tests/
│   ├── semantic_001_constant_folding.pi       # Демо свёртки констант
│   ├── semantic_002_unreachable_code.pi       # Демо удаления недостижимого кода
│   ├── semantic_003_if_simplification.pi      # Демо упрощения if
│   ├── semantic_004_unused_variables.pi       # Демо удаления неиспользуемых переменных
│   ├── semantic_005_return_outside_routine.pi # Демо ошибки: return вне функции
│   ├── semantic_006_all_optimizations.pi      # Комплексный тест всех оптимизаций
│   └── 006_use_before_declaration.pi          # Демо ошибки: использование до объявления
├── build/
│   └── semantic_analyzer          # Исполняемый файл
├── Makefile
└── run_semantic_tests.sh          # Скрипт для демонстрации

```

## Тестовые файлы

### Проверки (тесты с ошибками)

1. **tests/006_use_before_declaration.pi** - использование переменной до объявления
2. **tests/semantic_005_return_outside_routine.pi** - return вне функции

### Оптимизации (корректные тесты)

1. **tests/semantic_001_constant_folding.pi** - свёртка констант
2. **tests/semantic_002_unreachable_code.pi** - удаление недостижимого кода
3. **tests/semantic_003_if_simplification.pi** - упрощение if
4. **tests/semantic_004_unused_variables.pi** - удаление неиспользуемых переменных
5. **tests/semantic_006_all_optimizations.pi** - комплексный тест всех оптимизаций

## Пример вывода

```
Анализируется файл: tests/semantic_001_constant_folding.pi
✓ Синтаксический анализ пройден

=== Семантический анализ ===

Фаза 1: Семантические проверки
✓ Все декларации корректны
✓ Все переменные объявлены до использования
✓ return используется только внутри функций

Фаза 2: Оптимизации AST

📊 Статистика оптимизаций:
  • Свёртка констант: 5 операций
  • Удалено недостижимого кода: 0 блоков
  • Упрощение if: 0 конструкций
  • Удалено неиспользуемых переменных: 0
  Всего оптимизаций: 5

✅ Семантический анализ завершён успешно
```

## Архитектура анализатора

### Фаза 1: Семантические проверки
1. Обход AST с построением таблиц символов
2. Проверка объявлений до использования
3. Проверка корректности использования ключевых слов
4. Базовая проверка типов

### Фаза 2: Оптимизации AST
1. Свёртка константных выражений
2. Удаление недостижимого кода
3. Упрощение условных конструкций
4. Удаление неиспользуемых переменных

### Структура данных

```cpp
class SemanticAnalyzer {
    // Таблица символов с областями видимости
    vector<unordered_map<string, Symbol>> scopes;
    
    // Глобальная мапа использования переменных
    unordered_map<string, bool> globalUsage;
    
    // Счётчики оптимизаций
    int constantFoldingCount;
    int unreachableCodeCount;
    int ifSimplificationCount;
    int unusedVarCount;
};
```

## Требования к заданию

✅ **Минимум 2 проверки (не модифицируют AST):**
1. Объявление переменных и функций до использования
2. Проверка корректного использования return
3. Базовая проверка типов аргументов функций

✅ **Минимум 2 оптимизации (модифицируют AST):**
1. Свёртка констант
2. Удаление недостижимого кода
3. Упрощение условных конструкций
4. Удаление неиспользуемых переменных

## Презентация

Для презентации используйте:
1. `./run_semantic_tests.sh` - интерактивная демонстрация всех возможностей
2. Объяснение каждой проверки и оптимизации
3. Демонстрация статистики оптимизаций
4. Примеры корректных и некорректных программ

## Авторы

Проект выполнен для курса Compiler Construction, Innopolis University, 2025.

