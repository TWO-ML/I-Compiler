// [SEMANTICS] named reference types must be identical — ERROR
// Expect: assignment between different user-defined types is illegal

type R1 is record
  var x : integer
end

type R2 is record
  var x : integer
end

routine Main() is
  var a : R1
  var b : R2
  b.x := 7
  a := b   // different declared types => not allowed
end