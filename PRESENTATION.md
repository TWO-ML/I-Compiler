# Презентация: Семантический Анализатор

## Для демонстрации на лабораторной работе (6 ноября 2024)

---

## Быстрый запуск демонстрации

```bash
# Сборка
make build/semantic_analyzer

# Запуск интерактивной демонстрации
./run_semantic_tests.sh
```

---

## Что реализовано

### ✓ Проверки (3 проверки, не модифицируют AST)

1. **Объявление до использования** 
   - Все переменные и функции должны быть объявлены до использования
   - Тест: `tests/006_use_before_declaration.pi`

2. **Корректное использование return**
   - return только внутри функций
   - Тест: `tests/semantic_005_return_outside_routine.pi`

3. **Проверка типов аргументов**
   - Проверка количества аргументов при вызове функций

### ✓ Оптимизации (4 оптимизации, модифицируют AST)

1. **Свёртка констант** (5 операций на тесте)
   - `5 + 3` → `8`
   - `3 < 5` → `true`
   - Тест: `tests/semantic_001_constant_folding.pi`

2. **Удаление недостижимого кода** (2 блока на тесте)
   - Код после `return` удаляется
   - Тест: `tests/semantic_002_unreachable_code.pi`

3. **Упрощение if** (2 конструкции на тесте)
   - `if true then ... end` → тело then
   - `if false then ... else ... end` → тело else
   - Тест: `tests/semantic_003_if_simplification.pi`

4. **Удаление неиспользуемых переменных**
   - Тест: `tests/semantic_004_unused_variables.pi`

---

## Структура кода

```
src/semantic_analyzer.cpp (1300 строк)
├── Фаза 1: Семантические проверки
│   ├── checkDeclarationsAndUsage()  - проверка объявлений
│   └── Таблица символов с областями видимости
│
└── Фаза 2: Оптимизации AST
    ├── optimizeConstantFolding()     - свёртка констант
    ├── optimizeUnreachableCode()     - удаление недостижимого кода
    ├── optimizeIfSimplification()    - упрощение if
    └── optimizeUnusedVariables()     - удаление неиспользуемых переменных
```

---

## Пример вывода

```
=== Семантический анализ ===

Фаза 1: Семантические проверки
✓ Все декларации корректны
✓ Все переменные объявлены до использования
✓ return используется только внутри функций

Фаза 2: Оптимизации AST

📊 Статистика оптимизаций:
  • Свёртка констант: 5 операций
  • Удалено недостижимого кода: 0 блоков
  • Упрощение if: 2 конструкций
  • Удалено неиспользуемых переменных: 2
  Всего оптимизаций: 9

✅ Семантический анализ завершён успешно
```

---

## Демонстрационные тесты

| Тест | Что демонстрирует |
|------|------------------|
| `semantic_001_constant_folding.pi` | Свёртка 5 констант |
| `semantic_002_unreachable_code.pi` | Удаление 2 блоков после return |
| `semantic_003_if_simplification.pi` | Упрощение 2 if конструкций |
| `semantic_004_unused_variables.pi` | Удаление 2 неиспользуемых переменных |
| `semantic_005_return_outside_routine.pi` | ❌ Ошибка: return вне функции |
| `006_use_before_declaration.pi` | ❌ Ошибка: использование до объявления |
| `semantic_006_all_optimizations.pi` | 🎯 Все оптимизации вместе (7 оптимизаций) |

---

## Технические детали

### Таблица символов
- Стек областей видимости для вложенных функций
- Глобальная мапа использования переменных
- Отслеживание типов и параметров функций

### Алгоритм работы
1. **Синтаксический анализ** - построение AST
2. **Фаза 1** - обход AST, проверки, построение таблицы символов
3. **Фаза 2** - 4 прохода по AST для различных оптимизаций
4. **Вывод статистики** - количество примененных оптимизаций

---

## Проверка на существующих тестах

Семантический анализатор корректно работает на всех существующих тестах:
- `tests/001_expressions.pi` - ✓
- `tests/015_function_return_and_call.pi` - ✓
- `tests/006_use_before_declaration.pi` - ❌ (ожидаемая ошибка)

---

## Команды для демонстрации

### Пример 1: Свёртка констант
```bash
./build/semantic_analyzer tests/semantic_001_constant_folding.pi
```

### Пример 2: Ошибка - использование до объявления
```bash
./build/semantic_analyzer tests/006_use_before_declaration.pi
```

### Пример 3: Все оптимизации
```bash
./build/semantic_analyzer tests/semantic_006_all_optimizations.pi
```

### Полная демонстрация
```bash
./run_semantic_tests.sh
```

---

## Итоги

✅ Выполнены все требования:
- ✓ Минимум 2 проверки (реализовано 3)
- ✓ Минимум 2 оптимизации (реализовано 4)
- ✓ Демонстрационные тесты для каждой возможности
- ✓ Статистика оптимизаций
- ✓ Красивый вывод с эмодзи и цветами

**Всего:** 3 проверки + 4 оптимизации = 7 возможностей

---

## Вопросы для обсуждения

1. Как работает свёртка констант?
2. Как определяется недостижимый код?
3. Как построена таблица символов?
4. Какие еще оптимизации можно добавить?

---

**Готово к презентации! 🎉**

